const state = {
  count: 0,
};

// getters
const getters = {
  getCount: (state) => {
    return state.count;
  },
};

// mutations
const mutations = {};
// actions
const actions = {};

export default {
  // namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
